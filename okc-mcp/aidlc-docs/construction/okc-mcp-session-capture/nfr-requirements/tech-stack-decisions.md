# Session capture stack

Use existing TypeScript, Node stdio MCP SDK, Zod, YAML utilities and fast-check. No new dependencies or provider configuration.

Semantic routing runs in the connected coding agent under the shipped guide. Candidate retrieval is a bounded lexical aid with visible reasons. This keeps the existing no-internal-provider boundary and avoids claiming embedding/model quality.

